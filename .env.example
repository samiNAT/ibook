# Copy to .env.local and fill in real values. Never commit .env.local —
# .gitignore already excludes every .env* file.

# ---- Google Cloud ----------------------------------------------------------
# The GCP project this app targets. Project number is 839259427392.
# Authenticate locally with:  gcloud auth application-default login
GOOGLE_CLOUD_PROJECT=ibook-509112

# ---- Database --------------------------------------------------------------
# Not provisioned yet (KAN-13 / KAN-14). Postgres connection string.
# DATABASE_URL=postgresql://user:password@host:5432/ibook
